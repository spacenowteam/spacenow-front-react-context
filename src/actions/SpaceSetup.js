/* eslint-disable import/prefer-default-export */
// import { SPACE_SETUP_START } from '../constants'

// export const startSpaceSetupBySpaceId = parentId => dispatch => {
//   console.log(`Open space setup ${parentId}.`)
//   dispatch({ type: SPACE_SETUP_START })
// }
